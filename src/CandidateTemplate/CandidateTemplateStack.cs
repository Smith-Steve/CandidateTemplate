using Amazon.CDK;
using Constructs;

namespace CandidateTemplate
{
    public class CandidateTemplateStack : Stack
    {
        internal CandidateTemplateStack(Construct scope, string id, IStackProps props = null) : base(scope, id, props)
        {
            // The code that defines your stack goes here
        }
    }
}
